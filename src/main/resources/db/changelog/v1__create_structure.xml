<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
            http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ========================= -->
    <!-- USERS -->
    <!-- ========================= -->
    <changeSet id="001-create-users" author="alan">
        <createTable tableName="users">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="username" type="VARCHAR(40)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="email" type="VARCHAR(50)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="password" type="VARCHAR(120)">
                <constraints nullable="false"/>
            </column>
            <column name="active" type="BIT(1)" defaultValueBoolean="true"/>
            <column name="otp_verify" type="BIT(1)" defaultValueBoolean="false"/>
        </createTable>
    </changeSet>

    <!-- ========================= -->
    <!-- PROFILE (1-1 USERS) -->
    <!-- ========================= -->
    <changeSet id="002-create-profile" author="alan">
        <createTable tableName="profile">
            <column name="user_id" type="BIGINT">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="first_name" type="VARCHAR(255)"/>
            <column name="last_name" type="VARCHAR(255)"/>
            <column name="address" type="VARCHAR(255)"/>
            <column name="old" type="VARCHAR(255)"/>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="profile"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_profile_user"/>
    </changeSet>

    <!-- ========================= -->
    <!-- ROLES -->
    <!-- ========================= -->
    <changeSet id="003-create-roles" author="alan">
        <createTable tableName="roles">
            <column name="id" type="INT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(50)">
                <constraints nullable="false" unique="true"/>
            </column>
        </createTable>
    </changeSet>

    <!-- ========================= -->
    <!-- USER_ROLES (MANY-MANY) -->
    <!-- ========================= -->
    <changeSet id="004-create-user-roles" author="alan">
        <createTable tableName="user_roles">
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="role_id" type="INT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey
                tableName="user_roles"
                columnNames="user_id, role_id"
                constraintName="pk_user_roles"/>

        <addForeignKeyConstraint
                baseTableName="user_roles"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_user_roles_user"/>

        <addForeignKeyConstraint
                baseTableName="user_roles"
                baseColumnNames="role_id"
                referencedTableName="roles"
                referencedColumnNames="id"
                constraintName="fk_user_roles_role"/>
    </changeSet>

    <!-- ========================= -->
    <!-- OTP_CODE -->
    <!-- ========================= -->
    <changeSet id="005-create-otp-code" author="alan">
        <createTable tableName="otp_code">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="otp_code" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="otp_key" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="expiry_date" type="DATETIME(6)">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="otp_code"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_otp_user"/>
    </changeSet>

    <!-- ========================= -->
    <!-- REFRESH_TOKEN -->
    <!-- ========================= -->
    <changeSet id="006-create-refresh-token" author="alan">
        <createTable tableName="refresh_token">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="token" type="VARCHAR(255)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="expiry_date" type="DATETIME(6)">
                <constraints nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="refresh_token"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_refresh_token_user"/>
    </changeSet>

    <!-- ========================= -->
    <!-- EMAIL_RECORD -->
    <!-- ========================= -->
    <changeSet id="007-create-email-record" author="alan">
        <createTable tableName="email_record">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="to_email" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="subject" type="VARCHAR(255)"/>
            <column name="body" type="TEXT"/>
            <column name="sent_at" type="DATETIME(6)"/>
        </createTable>
    </changeSet>

    <!-- ========================= -->
    <!-- EMAIL_RECORD_SEQ -->
    <!-- ========================= -->
    <changeSet id="008-create-email-record-seq" author="alan">
        <createTable tableName="email_record_seq">
            <column name="next_val" type="BIGINT"/>
        </createTable>
    </changeSet>

</databaseChangeLog>
